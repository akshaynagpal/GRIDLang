void addToGrid(int x, int y, Player listNode p_n){
		*Player listNode iterator;
		if (GridNew[x][y] == None)
		{
			print("Hi");
			GridNew[x][y] = &p_n;
			/*Not working*/
/*			GridNew[x][y].next = None;*/
			return;
		}

}

