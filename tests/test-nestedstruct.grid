Player good
{ 
int x; 
int y; 
string s; 
Item horse h1;
} 

Item horse
{ 
int x; 
int y; 
string s; 

} 

int gameloop()
{ 
Player good p1; 
p1.x = 10;
p1.s = "nested structs work";
p1.h1.x = 100;
p1.h1.y = 200;
p1.h1.s = "nested horse works";

print(p1.x);
print(p1.s);
print(p1.h1.x);
print(p1.h1.y);
print(p1.h1.s);

return 0; 
}
