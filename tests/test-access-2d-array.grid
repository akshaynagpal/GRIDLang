import gridBasics.grid

int checkGameEnd()
{
  return 1;
}

Player
{ 
  string name;
} 

int colocation(int x, int y, Item listNode i1, Item listNode i2)
{
  return 0;
}

Grid_Init<0,0> grid; 
int playerOrderSize;

int initialSetup(){ 
  playerOrderSize = 2;
  return 0;
}

int gameloop(){
	int[2,2] y;
    y[0,0] = 0;
    y[0,1] = 1;
    y[1,0] = 2;
    y[1,1] = 3;
    print(y[0,1]);
    return 0;
}