import gridBasics.grid

int checkGameEnd()
 {
  return 1;
 }

Player
{ 
  int x; 
  int y; 
  string s; 
  Player* next;
} 

int colocation(int x, int y, Item listNode i1, Item listNode i2)
{
  return 0;
}

Grid_Init<0,0> grid; 
int playerOrderSize;

int initialSetup(){ 
  playerOrderSize = 2;
  return 0;
}

int gameloop()
{ 
Player p1; 
Player p2; 
Player p3; 
Player[2] parray; 
p2.x = 3;

parray[0] = p1;
parray[0].next = &p2;
p3 = *(parray[0].next);
print(p3.x);


return 0; 
}