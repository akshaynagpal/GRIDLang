Item horse {
	int x;
	string s;
	*Item horse next;
	int rule(coordinate c1, coordinate c2) {
		return 1;
	}
}

Player good
{ 
  int x; 
  int y; 
  string s; 
}

int checkGameEnd()
{
	return 1;
}

int initialSetup(){ 
	return 0;
}

int gameloop(){ 
  Player good p1;
  Player good p2;
  Player good[2] playerOrder;
	playerOrder = [p1,p2];
  /*z = y[currentPlayerIndex];*/
	print(x);
	return 0;
}