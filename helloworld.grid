Player good
{ 
int x; 
int y; 
string s; 
} 

int gameloop()
{ 
Player good p1; 
Player good p2; 
Player good p3;
Player good[2] parray; 
p1.x = 3;
parray[0] = p1;
p3 = parray[0];
print(parray[0].x);
return 0; 
}
