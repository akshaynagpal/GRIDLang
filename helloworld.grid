Item horse {
	int x;
	string s;
	*Item horse next;
}

int checkGameEnd()
{
	return 1;
}

int initialSetup(){ 
	return 0;
}

int gameloop(){ 
	int x;
	int[4] y;
	int z;
	x = 1;
	y = [1,2,3,4];
    z = y[currentPlayerIndex];
	print(z);
	return 0;
}