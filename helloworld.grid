
int checkGameEnd()
 {
  return 1;
 }

Player
{ 
} 

int colocation(int x, int y, Item listNode* i1, Item listNode* i2)
{
  return 0;
}

int playerOrderSize;

int initialSetup(){ 
  playerOrderSize = 1;
  return 0;
}

int gameloop(){ 
  print("Hello world");
  return 0; 
}