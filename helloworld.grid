import gridBasics.grid;

int checkGameEnd()
 {
  return 1;
 }


Player good
{ 
	int x; 
	int y; 
	string s; 
	*Player good next;
} 


Item horse{
	int x;
	string s;
	*Item horse next;
	int rule(coordinate c1, coordinate c2) {
		return 1;
	}
}

Player grid{
	Player listNode node_head;
}

Player something{
  *Player good good_node;  
}

int gameloop(){ 
	Player good p1; 
  Player something abc;
	Grid_Init <8,8>; 
/*  abc.good_node = &p1;*/
	Grid<0,0> <-- p1;
	return 0; 


}
